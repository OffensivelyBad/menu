import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  addButtonContainer: {
    margin: 8,
  },
  addButton: {
    fontSize: 28,
    color: '#0179fe',
  },
  textButton: {
    fontSize: 16,
    color: '#0179fe',
  },
});

export default styles;
